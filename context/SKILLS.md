# Agent Skills

Last updated: 2026-07-03

## Required Skill Stack For MCP Maker

These skills are already available locally and are sufficient for the current MCP Maker design and implementation workflow.

| Skill | Use |
|---|---|
| `design-taste-frontend` | Product UI direction, anti-generic frontend design, visual coherence |
| `image-to-code` | Faithful implementation from `_proto` references |
| `impeccable` | UI review, accessibility, layout polish, state completeness |
| `frontend-design` | Typography, hierarchy, interaction design support |
| `high-end-visual-design` | Premium visual quality guardrails |
| `imagegen-frontend-web` | Future web section concept generation if a new visual reference is needed |
| `imagegen-frontend-mobile` | Future mobile screen concept generation if a new visual reference is needed |
| `brandkit` | Brand boards and identity direction if MCP Maker branding is expanded |
| `full-output-enforcement` | Exhaustive generation tasks where placeholders are unacceptable |
| `openai-docs` | Official OpenAI/Codex documentation checks when needed |
| `skill-installer` | Install curated or GitHub-hosted skills when a missing skill is explicitly identified |
| `skill-creator` | Create custom Codex skills for MCP Maker-specific workflows if needed |
| `find-skills` | Discover candidate skills when new capability needs appear |

## Optional Style Skills

Use these only when their direction is explicitly desired:

- `minimalist-ui`
- `industrial-brutalist-ui`
- `gpt-taste`
- `stitch-design-taste`
- `ui-ux-pro-max`
- `redesign-existing-projects`

## Current Installation Status

No additional skill installation is required for the immediate `_proto` implementation handoff. The required skills are already present in the local skill directories.

Remote curated skill discovery through `skill-installer` failed again on 2026-07-03 because Python certificate verification failed against GitHub. Do not bypass TLS verification. If a specific remote skill is required later, install it by exact GitHub path or fix the local Python certificate chain first.
